# 13. Экран треков: плитки и картинки

**Статус:** in-progress
**Обновлено:** 2026-08-09
**Проект:** frontend
**Зависит от:** 04, 05, 06, 12

## Цель

Главный экран: треки в виде плиток (тайлов), с картинкой (или заглушкой),
названием, длительностью; фильтр/переключение по группам; действия
(переименовать, удалить, перенести в группу, прикрепить фото).

## Детали

1. **Сетка плиток** — адаптивная (`grid-cols-2` на узких экранах, больше на
   планшетах), картинка как фон плитки (`aspect-square` или `aspect-[4/3]`),
   название и длительность — оверлеем снизу (градиент для читаемости поверх
   фото, как в типичных медиатеках).
2. **Заглушка без фото** — не пустой квадрат, а что-то с намёком на аудио
   (иконка ноты/волны + акцентный цвет по хэшу имени файла, чтобы плитки без
   фото не выглядели одинаково-безликими).
3. **Прикрепление фото** — долгий тап/кнопка на плитке открывает bottom sheet
   с тремя вариантами (буфер обмена / камера / файл), см. план 06. Показать
   превью и кнопку "Сохранить" перед отправкой на устройство (дать
   возможность отменить/выбрать другое фото до аплоада).
4. **Группы** — горизontal-scroll чипов сверху экрана (по образцу
   Instagram/Stories-подобных фильтров), выбор группы фильтрует сетку.
   "Перенести в группу" — из контекстного меню плитки, список существующих
   групп + "Создать новую".
5. **Переименование/удаление** — контекстное меню (долгий тап или кнопка
   "⋮" на плитке), удаление — через `Dialog`-подтверждение.
6. **Долгая загрузка списка** (потенциально десятки треков с картинками) —
   предусмотреть скелетоны/лоадер, картинки — `loading="lazy"`.
7. **Превью-воспроизведение** (новое требование) — кнопка ▶/⏸ прямо на
   плитке (поверх картинки/заглушки), вызывает `POST /api/tracks/play` /
   `/stop` (план 04). Играет только один трек одновременно — при старте
   нового превью визуально сбросить состояние play-кнопки на плитке, где
   играло раньше (опрос `GET /api/status`-подобного эндпоинта или локальный
   state — решить при реализации). Учесть, что это отдельная функция от
   "добавить в сценарий" — не путать плей-превью с чекбоксами выбора треков
   в редакторе сценария (план 14).

## Открытые вопросы

- Нет.

## Критерии готовности

- На реальном мобильном устройстве, подключённом к AP, экран плиток
  отзывчивый, без дёрганий при скролле, действия (rename/delete/group/photo)
  работают end-to-end против реального API устройства.

## Как реализовано (частично)

Сделан рабочий экран управления треками (`frontend/src/screens/TracksScreen.tsx`
+ `TrackCard`/`GroupFilterChips`/`GroupsManager`/`ConfirmDialog`), закрывающий
пункты 1 (частично, без фото-фона), 4, 5, 7 и критерии по rename/delete/group
(без photo — план 06 на устройстве ещё не реализован, фото прикреплять
физически неоткуда):

- Карточка трека вместо плитки-с-фоном (нет картинки — нет и смысла в
  `aspect-square` сетке из п.1/2 черновика) — название (inline-переименование
  по иконке карандаша), длительность/размер бейджами, ▶/⏸ превью
  (`/api/tracks/play`/`stop`, план 04), select группы, удаление через
  `ConfirmDialog`.
- Группы — чипы-фильтр сверху (`GroupFilterChips`, п.4) + отдельная секция
  управления (`GroupsManager`: создать/переименовать/удалить группу,
  скрыта за кнопкой "Группы", не смешана с фильтром).
- Загрузка трека — кнопка + `<input type=file>`, клиентская валидация
  расширения/размера как быстрая обратная связь (финальная — на
  устройстве, план 04/`CLAUDE.md` §4.5.1), прогресс через XHR.
- "Очистить всё" — кнопка + подтверждение, `POST /api/clear_all`.
- Данные — `useDeviceData()` (`src/hooks/useDeviceData.ts`), polling
  `/api/status`+`/api/groups` каждые 2с — открытый вопрос плана ("опрос или
  локальный state") решён в пользу опроса: единый источник истины при
  нескольких клиентах (телефон + ноутбук одновременно), не расходится.
- Тесты: `device.test.ts` (API-клиент), `TracksScreen.test.tsx` (рендер
  списка, play, delete-через-подтверждение, фильтр по группе, обработка
  ошибки связи), `format.test.ts`.

**Обновление (план 06 закрыт) — пп. 2/3 тоже сделаны:**
- Заглушка без фото — акцентный цвет по хэшу имени (`lib/hashColor.ts`) +
  иконка ноты (`lucide-react`), сверху карточки.
- Прикрепление фото — `TrackImagePicker` (буфер обмена / камера / файл),
  ресайз на канвасе (`lib/image.ts`, до 600px/JPEG 80%), превью + кнопка
  "Сохранить" перед отправкой. **Не** bottom sheet (`vaul`/`Drawer`) — обычный
  `Dialog`, тот же компромисс "функционал сейчас, визуал — отдельный проход
  по плану 12" (подтверждено пользователем явно).
- Подробности реализации — в `Plan/06-image-upload-and-storage.done.md`.

**Не сделано (осознанно отложено):**
- Плитка-с-фоном на всю карточку (`aspect-square`/градиент-оверлей снизу,
  п.1) — картинка сейчас сверху карточки отдельным блоком, не фон целиком;
  визуальный проход по плану 12 сделает это точнее.
- Скелетоны — есть только на первую загрузку экрана (`Skeleton`), не на
  отдельные картинки, `loading="lazy"` не выставлен.
- **Не проверено на реальном мобильном устройстве** (плиточная
  отзывчивость, скролл, все три источника фото с телефона) — сделано только
  `npm run build`+`npm run test:run` (зелёные) и частичная живая проверка
  части функций (play/rename/delete/группы/upload аудио) — план 17.
